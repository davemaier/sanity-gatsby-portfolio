import React from 'react'

const Contact = ({children}) => (
    
    <div>
        <a name="contact"></a>
        <h2>
            Let's talk about your project!

            Ta
        </h2>
    </div>
);

export default Contact;
